import { GreenblackDirective } from './greenblack.directive';

describe('GreenblackDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenblackDirective();
    expect(directive).toBeTruthy();
  });
});
